export default function HomePage() {
  return (
    <section className="space-y-8">
      <p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">
        Phase 1 Scaffold
      </p>
      <h1 className="max-w-3xl text-4xl font-bold leading-tight text-foreground sm:text-5xl">
        I design and build digital products with strong UX and robust engineering.
      </h1>
      <p className="max-w-2xl text-lg text-text-muted">
        This marketing shell is now wired. Next steps are component stubs, content pages, and
        CI/test setup from the Phase 1 plan.
      </p>
    </section>
  );
}
